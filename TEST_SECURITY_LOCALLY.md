# 🧪 Тестирование безопасности локально

## ✅ Можно тестировать без пересборки!

Изменения в коде автоматически применяются в dev режиме. Просто перезапустите dev серверы.

---

## 📱 Mobile приложение (Expo)

### Запуск в dev режиме:

```bash
# Из корня проекта
pnpm dev:mobile

# Или из папки mobile
cd apps/mobile
pnpm start
```

### Тестирование:

1. **Откройте приложение в Expo Go** (скачайте из App Store/Google Play)
2. **Отсканируйте QR-код** из терминала
3. **Создайте 2 учетные записи** и войдите под каждой
4. **Проверьте изоляцию:**
   - Создайте задачи в первой учетной записи
   - Войдите во вторую учетную запись
   - Убедитесь, что задачи первой не видны
   - Попробуйте удалить задачу (через UI) - должна быть доступна только своя

---

## 💻 Desktop приложение (Tauri)

### Запуск в dev режиме:

```bash
# Из корня проекта
pnpm dev:desktop

# Или из папки desktop
cd apps/desktop
pnpm tauri:dev
```

### Тестирование:

1. **Приложение запустится автоматически** (Tauri окно)
2. **Создайте 2 учетные записи** и войдите под каждой
3. **Проверьте изоляцию:**
   - Создайте задачи в первой учетной записи
   - Выйдите и войдите во вторую учетную запись
   - Убедитесь, что задачи первой не видны
   - Попробуйте удалить задачу - должна быть доступна только своя

---

## 🔍 Что проверить

### Тест 1: Изоляция задач
- [ ] Пользователь 1 создает задачи
- [ ] Пользователь 1 видит только свои задачи
- [ ] Пользователь 2 не видит задачи пользователя 1
- [ ] Пользователь 2 видит только свои задачи

### Тест 2: Удаление задач
- [ ] Пользователь 1 может удалить свою задачу
- [ ] Пользователь 1 не может удалить задачу пользователя 2 (если бы видел ID)
- [ ] При попытке удалить чужую задачу - ошибка "Access denied"

### Тест 3: Обновление задач
- [ ] Пользователь 1 может обновить свою задачу
- [ ] Пользователь 1 не может обновить задачу пользователя 2
- [ ] При попытке обновить чужую задачу - ошибка "Access denied"

### Тест 4: Создание задач
- [ ] Созданная задача автоматически привязывается к текущему пользователю
- [ ] Другие пользователи не видят новую задачу

---

## 🐛 Проверка логов

В dev режиме все проверки безопасности логируются в консоль:

### Desktop:
- Откройте DevTools (F12 или правый клик → Inspect)
- Смотрите вкладку Console

### Mobile:
- Логи в терминале где запущен `pnpm start`
- Или используйте React Native Debugger

### Что искать в логах:

**Успешные операции:**
```
[TaskStore] Loaded X tasks
[useSync] Loaded X tasks from cache
```

**Блокированные попытки (если тестируете API напрямую):**
```
[TaskStore] SECURITY: Attempt to delete task belonging to another user: ...
[useSync] SECURITY: Attempt to update task belonging to another user: ...
Access denied: Cannot delete tasks belonging to other users
```

---

## ⚠️ Важно: Настройте RLS в Supabase

**Перед тестированием убедитесь, что RLS настроен в Supabase!**

Если RLS не настроен, то:
- ❌ Код приложения защищен, но база данных нет
- ❌ Можно обойти защиту через прямой доступ к API
- ⚠️ Небезопасно для production

**См. `SECURITY_SETUP.md` для инструкций по настройке RLS**

---

## 🔄 Перезапуск после изменений

Если вы внесли изменения в код:

1. **Остановите dev сервер** (Ctrl+C)
2. **Перезапустите:**
   ```bash
   # Mobile
   pnpm dev:mobile
   
   # Desktop
   pnpm dev:desktop
   ```

TypeScript автоматически перекомпилируется при запуске.

---

## 📝 Быстрая проверка

**Минимальный тест (1 минута):**

1. Запустите приложение в dev режиме
2. Войдите под первой учетной записью
3. Создайте задачу "Test Task 1"
4. Выйдите и войдите под второй учетной записью
5. ✅ Убедитесь, что "Test Task 1" не видна

Если тест пройден - изоляция работает! 🎉

---

## 🚀 Production build (когда будете готовы)

Для production все равно нужно пересобрать:

```bash
# Desktop
cd apps/desktop
pnpm tauri:build

# Mobile (через EAS)
cd apps/mobile
pnpm build:android
```

Но для **тестирования безопасности** достаточно dev режима!

