# 🔐 EAS Build - Environment Variables Setup

## ✅ Исправлено

Исправлен `eas.json` - удалены пустые env переменные и некорректная iOS конфигурация.

---

## 🔧 Настройка переменных окружения

Для EAS Build нужно установить переменные окружения **через EAS Secrets** (рекомендуется) или через `.env` файл.

### Вариант 1: EAS Secrets (Рекомендуется) ✅

Этот метод безопасен и переменные будут доступны во время build на серверах Expo.

#### Шаг 1: Установите переменные через EAS CLI

```bash
cd apps/mobile

# Установить Supabase URL
npx eas-cli secret:create --scope project --name EXPO_PUBLIC_SUPABASE_URL --value "your_supabase_url_here"

# Установить Supabase Key
npx eas-cli secret:create --scope project --name EXPO_PUBLIC_SUPABASE_KEY --value "your_supabase_anon_key_here"
```

#### Шаг 2: Проверьте установленные секреты

```bash
npx eas-cli secret:list
```

#### Шаг 3: Обновите eas.json (если нужно)

Если используете EAS Secrets, можно удалить блок `env` из `eas.json` (уже сделано).

---

### Вариант 2: Через eas.json (если нужны разные значения для разных профилей)

Если вы хотите использовать разные значения для разных профилей, обновите `eas.json`:

```json
{
  "build": {
    "production": {
      "android": {
        "buildType": "apk"
      },
      "env": {
        "EXPO_PUBLIC_SUPABASE_URL": "https://your-project.supabase.co",
        "EXPO_PUBLIC_SUPABASE_KEY": "your-anon-key-here"
      }
    }
  }
}
```

**⚠️ Важно**: Не коммитьте реальные ключи в git! Используйте EAS Secrets или `.env` файл.

---

### Вариант 3: Локальный .env файл (для локальной разработки)

Для локальной разработки создайте `.env` файл в `apps/mobile/`:

```bash
# apps/mobile/.env
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_KEY=your-anon-key-here
```

Убедитесь, что `.env` добавлен в `.gitignore` (обычно уже есть).

---

## 🚀 После настройки переменных

Запустите build снова:

```bash
pnpm build:android
```

---

## 📝 Где взять значения?

1. Откройте ваш Supabase проект
2. Перейдите в Settings → API
3. Скопируйте:
   - **Project URL** → `EXPO_PUBLIC_SUPABASE_URL`
   - **anon/public key** → `EXPO_PUBLIC_SUPABASE_KEY`

---

## ✅ Проверка

После установки переменных через EAS Secrets, они автоматически будут доступны во время build. Проверьте:

```bash
npx eas-cli secret:list
```

Должны увидеть оба секрета в списке.

---

## 🔒 Безопасность

- ✅ **Используйте EAS Secrets** для production builds
- ❌ **НЕ коммитьте** реальные ключи в `eas.json`
- ❌ **НЕ коммитьте** `.env` файлы с реальными ключами
- ✅ Используйте `.env.example` для документации

