# 🧪 Инструкции по тестированию безопасности

## ✅ Да, я могу протестировать автоматически!

Я создал автоматизированный скрипт для тестирования безопасности. Вам **НЕ нужно** запускать приложения вручную.

---

## 🔐 Варианты предоставления данных

### Вариант 1: Через переменные окружения (рекомендуется)

Создайте файл `.env` в корне проекта (или `.env.test`):

```bash
# Supabase credentials
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-anon-key

# Test user 1
TEST_USER1_EMAIL=user1@example.com
TEST_USER1_PASSWORD=password1

# Test user 2
TEST_USER2_EMAIL=user2@example.com
TEST_USER2_PASSWORD=password2
```

**Важно:** Добавьте `.env` в `.gitignore`, чтобы не коммитить пароли!

### Вариант 2: Через аргументы командной строки

```bash
npx tsx test-security-api.ts \
  user1@example.com password1 \
  user2@example.com password2
```

### Вариант 3: Через секреты (если используете GitHub Actions или подобное)

Установите переменные окружения в секретах CI/CD.

---

## 🚀 Запуск тестов

### Шаг 1: Установите зависимости (если еще не установлены)

```bash
pnpm install
```

### Шаг 2: Установите tsx (для запуска TypeScript)

```bash
pnpm add -D tsx
```

Или из корня:
```bash
cd apps/desktop
pnpm add -D tsx
```

### Шаг 3: Настройте переменные окружения

Создайте `.env` файл в корне проекта с данными двух пользователей.

### Шаг 4: Запустите тесты

```bash
npx tsx test-security-api.ts
```

Или если переменные окружения уже установлены:
```bash
npx tsx test-security-api.ts user1@email.com pass1 user2@email.com pass2
```

---

## 📋 Что тестирует скрипт

1. ✅ **Аутентификация** обоих пользователей
2. ✅ **Создание задач** каждым пользователем
3. ✅ **Видимость задач** - пользователь видит только свои задачи
4. ✅ **Изоляция задач** - пользователи НЕ видят чужие задачи (КРИТИЧЕСКИЙ ТЕСТ)
5. ✅ **RLS на удаление** - пользователь НЕ может удалить чужую задачу
6. ✅ **RLS на обновление** - пользователь НЕ может обновить чужую задачу
7. ✅ **Полная изоляция** - финальная проверка

---

## 🔒 Безопасность данных

**Важно:** Не отправляйте пароли напрямую в чат!

### Рекомендуемый способ:

1. Создайте файл `.env` локально
2. Запустите тесты локально
3. Пришлите мне только результаты тестов (output скрипта)
4. Или используйте временные тестовые учетные записи

---

## 📊 Пример вывода

После запуска вы увидите что-то вроде:

```
🔒 Starting Security Tests for MyDailyOps

============================================================

📝 Authenticating User 1...
✅ User 1 Authentication
   Authenticated as user1@example.com

📝 Authenticating User 2...
✅ User 2 Authentication
   Authenticated as user2@example.com

🧪 Test 1: User 1 creates a task...
✅ Test 1: User 1 creates task
   Task created: test-1234567890-1

🧪 Test 2: User 1 can see their own task...
✅ Test 2: User 1 can see own tasks
   User 1 can see 5 tasks including their test task

🧪 Test 3: User 2 cannot see User 1's task (CRITICAL TEST)...
✅ Test 3: User 2 cannot see User 1's task
   User 2 can see 3 tasks, but NOT User 1's task

🧪 Test 4: User 2 tries to delete User 1's task (should fail)...
✅ Test 4: User 2 cannot delete User 1's task
   RLS blocked deletion: new row violates row-level security policy

🧪 Test 5: User 2 tries to update User 1's task (should fail)...
✅ Test 5: User 2 cannot update User 1's task
   RLS blocked update: new row violates row-level security policy

🧪 Test 6: User 2 creates their own task...
✅ Test 6: User 2 creates task
   Task created: test-1234567890-2

🧪 Test 7: Verify isolation between users...
✅ Test 7: Task isolation verified
   User 2 can see their own task but NOT User 1's task ✅

🧹 Cleaning up test tasks...
✅ Cleanup complete

============================================================
📊 Test Summary
============================================================
Total tests: 7
✅ Passed: 7
❌ Failed: 0

============================================================
🎉 All security tests passed!
```

---

## ⚠️ Что делать, если тесты не проходят

### Если тест 3 или 7 не проходит:

**Проблема:** RLS не настроен в Supabase.

**Решение:** 
1. Откройте `SECURITY_SETUP.md`
2. Настройте RLS политики в Supabase Dashboard
3. Запустите тесты снова

### Если тест 4 или 5 не проходит:

**Проблема:** RLS политики на DELETE/UPDATE не работают.

**Решение:**
1. Проверьте политики в Supabase Dashboard
2. Убедитесь, что они созданы правильно
3. См. `SECURITY_SETUP.md` для SQL команд

### Если аутентификация не проходит:

**Проблема:** Неправильные учетные данные или пользователи не существуют.

**Решение:**
1. Проверьте email и пароли
2. Убедитесь, что пользователи зарегистрированы в Supabase
3. Попробуйте войти через приложение вручную

---

## 🎯 Альтернатива: Я могу проанализировать код

Если вы не хотите делиться паролями, я могу:

1. ✅ Проверить код на наличие всех проверок безопасности
2. ✅ Проанализировать SQL запросы
3. ✅ Проверить логику изоляции пользователей
4. ✅ Дать рекомендации по улучшению

**Но для полной уверенности лучше запустить автоматизированные тесты!**

---

## 📝 Следующие шаги

1. Создайте `.env` файл с учетными данными
2. Запустите `npx tsx test-security-api.ts`
3. Пришлите мне результаты
4. Я проанализирую и дам рекомендации

**Готов начать тестирование, как только предоставите данные!** 🚀

