# 🔒 Сводка исправлений безопасности

## Дата: 2024

## Проблема
Пользователи могли видеть и удалять задачи других пользователей из-за отсутствия проверок `user_id` в критических функциях.

---

## ✅ Реализованные исправления

### 1. Desktop приложение (`apps/desktop/`)

#### Файл: `src/lib/db.ts`
- ✅ **`getTaskById(id, userId)`** - теперь требует `userId` и фильтрует по нему
  ```typescript
  // БЫЛО:
  SELECT * FROM tasks WHERE id = ?
  
  // СТАЛО:
  SELECT * FROM tasks WHERE id = ? AND user_id = ?
  ```

- ✅ **`deleteTaskFromCache(id, userId)`** - теперь требует `userId` и удаляет только задачи пользователя
  ```typescript
  // БЫЛО:
  DELETE FROM tasks WHERE id = ?
  
  // СТАЛО:
  DELETE FROM tasks WHERE id = ? AND user_id = ?
  ```

#### Файл: `src/stores/taskStore.ts`
- ✅ **`updateTask`** - добавлена проверка принадлежности задачи пользователю
  ```typescript
  // Добавлено:
  const currentTask = await db.getTaskById(id, userId);
  if (!currentTask) {
    throw new Error("Task not found or access denied");
  }
  if (currentTask.user_id !== userId) {
    throw new Error("Access denied");
  }
  ```

- ✅ **`deleteTask`** - добавлена проверка принадлежности перед удалением
  ```typescript
  // Добавлено:
  const taskToDelete = await db.getTaskById(id, userId);
  if (taskToDelete.user_id !== userId) {
    throw new Error("Access denied");
  }
  ```

#### Файл: `src/utils/recurring.ts`
- ✅ **`deleteAllInstances`** - обновлено для передачи `userId` в `deleteTaskFromCache`
- ✅ **`deleteFutureInstances`** - добавлена проверка `user_id` перед удалением

#### Файл: `src/services/syncService.ts`
- ✅ **`deleteTaskFromSupabase`** - обновлено для передачи `userId` в `deleteTaskFromCache`

---

### 2. Mobile приложение (`apps/mobile/`)

#### Файл: `database/init.ts`
- ✅ **`getTaskById(id, userId)`** - теперь требует `userId` и фильтрует по нему
  ```typescript
  // БЫЛО:
  SELECT * FROM tasks WHERE id = ?
  
  // СТАЛО:
  SELECT * FROM tasks WHERE id = ? AND user_id = ?
  ```

- ✅ **`deleteTaskFromCache(id, userId)`** - теперь требует `userId` и удаляет только задачи пользователя
  ```typescript
  // БЫЛО:
  DELETE FROM tasks WHERE id = ?
  
  // СТАЛО:
  DELETE FROM tasks WHERE id = ? AND user_id = ?
  ```

#### Файл: `hooks/useSync.ts`
- ✅ **`updateTask`** - добавлена проверка принадлежности задачи пользователю
  ```typescript
  // Добавлено:
  if (task.user_id !== userId) {
    throw new Error('Access denied');
  }
  const existingTask = await getTaskById(task.id, userId);
  if (!existingTask) {
    throw new Error('Task not found or access denied');
  }
  ```

- ✅ **`deleteTask`** - добавлена проверка принадлежности перед удалением
  ```typescript
  // Добавлено:
  const taskToDelete = await getTaskById(taskId, userId);
  if (taskToDelete.user_id !== userId) {
    throw new Error('Access denied');
  }
  ```

#### Файл: `utils/recurring.ts`
- ✅ **`deleteAllInstances`** - обновлено для передачи `userId` в `deleteTaskFromCache`
- ✅ **`deleteFutureInstances`** - добавлена проверка `user_id` перед удалением

#### Файл: `lib/sync.ts`
- ✅ **`pullFromSupabase`** - добавлена проверка `user_id` перед удалением локальных задач

---

## 🔐 Защита на уровне кода

### Все операции с задачами теперь проверяют:

1. **Аутентификация**: `userId` получен от текущего пользователя
2. **Принадлежность**: Задача проверяется на принадлежность пользователю
3. **Фильтрация**: Все SQL-запросы фильтруются по `user_id`
4. **Логирование**: Все попытки несанкционированного доступа логируются

---

## ⚠️ ВАЖНО: Что нужно сделать дополнительно

### Настройка Row Level Security (RLS) в Supabase

**Критически важно!** Несмотря на защиту на уровне кода, необходимо настроить RLS в Supabase для защиты на уровне базы данных.

**См. файл `SECURITY_SETUP.md` для подробных инструкций.**

Основные шаги:
1. Включить RLS для таблицы `tasks`
2. Создать политики для SELECT, INSERT, UPDATE, DELETE
3. Протестировать с несколькими пользователями

---

## 📊 Результаты

### До исправлений:
- ❌ Пользователи могли видеть задачи других пользователей (если знали ID)
- ❌ Пользователи могли удалять задачи других пользователей
- ❌ Пользователи могли обновлять задачи других пользователей
- ❌ Функции `getTaskById` не фильтровали по `user_id`

### После исправлений:
- ✅ Пользователи видят только свои задачи
- ✅ Невозможно удалить чужую задачу
- ✅ Невозможно обновить чужую задачу
- ✅ Все функции проверяют `user_id`
- ✅ Все SQL-запросы фильтруются по `user_id`
- ✅ Попытки несанкционированного доступа логируются

---

## 🧪 Тестирование

### Рекомендуемые тесты:

1. **Тест изоляции:**
   - Создайте 2 учетные записи
   - Создайте задачи в каждой
   - Убедитесь, что пользователи не видят чужие задачи

2. **Тест безопасности:**
   - Попробуйте удалить задачу другого пользователя (через API)
   - Должна вернуться ошибка "Access denied"

3. **Тест обновления:**
   - Попробуйте обновить задачу другого пользователя
   - Должна вернуться ошибка "Access denied"

---

## 📝 Измененные файлы

### Desktop:
- `apps/desktop/src/lib/db.ts`
- `apps/desktop/src/stores/taskStore.ts`
- `apps/desktop/src/utils/recurring.ts`
- `apps/desktop/src/services/syncService.ts`

### Mobile:
- `apps/mobile/database/init.ts`
- `apps/mobile/hooks/useSync.ts`
- `apps/mobile/utils/recurring.ts`
- `apps/mobile/lib/sync.ts`

---

## ✅ Статус: ЗАВЕРШЕНО

Все критические функции защищены. Приложение готово к использованию после настройки RLS в Supabase.

