# 🔧 EAS Build Windows Permission Fix - Complete Solution

## Проблема
```
EPERM: operation not permitted, rmdir 'C:\Users\ivany\AppData\Local\Temp\eas-cli-nodejs\...'
Failed to upload the project tarball to EAS Build
```

---

## ✅ Решение 0: Использовать Linux/Ubuntu (Самый надежный способ)

**Если у вас установлен WSL (Windows Subsystem for Linux) или доступ к Ubuntu:**

1. Откройте Ubuntu (WSL или удаленный сервер)
2. Перейдите в директорию проекта:
   ```bash
   cd /mnt/c/Dev/MyDailyOps/apps/mobile
   # или на удаленном сервере:
   # git clone <your-repo> && cd apps/mobile
   ```
3. Установите зависимости:
   ```bash
   pnpm install
   ```
4. Войдите в EAS (если еще не вошли):
   ```bash
   pnpm eas:login
   ```
5. Запустите build:
   ```bash
   pnpm build:android
   ```

**Преимущества:**
- ✅ Нет проблем с правами доступа
- ✅ Нет конфликтов с антивирусом
- ✅ Более стабильная работа EAS CLI
- ✅ Быстрее и надежнее

**Примечание:** Если используете WSL, убедитесь, что Node.js и pnpm установлены в Ubuntu окружении:
```bash
# В Ubuntu/WSL
curl -fsSL https://get.pnpm.io/install.sh | sh -
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install --lts
```

---

## ✅ Решение 1: Используйте готовый скрипт (Рекомендуется)

### Шаг 1: Запустите скрипт очистки

```powershell
cd apps/mobile
.\clear-eas-cache.ps1
```

Или через npm:
```bash
pnpm eas:clear-cache
```

**Если скрипт не запускается**, выполните в PowerShell от имени администратора:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
cd apps/mobile
.\clear-eas-cache.ps1
```

### Шаг 2: Запустите build от имени администратора

1. Закройте все терминалы
2. Откройте PowerShell от имени администратора
3. Запустите build:

```bash
cd C:\Dev\MyDailyOps\apps\mobile
pnpm build:android
```

---

## ✅ Решение 2: Ручное удаление временных папок

### Вариант A: Через PowerShell (от имени администратора)

```powershell
# Закройте все терминалы и процессы Node/EAS сначала!

# Удалить основную папку
Remove-Item -Recurse -Force "$env:LOCALAPPDATA\Temp\eas-cli-nodejs" -ErrorAction SilentlyContinue

# Удалить альтернативную папку (если есть)
Remove-Item -Recurse -Force "$env:TEMP\eas-cli-nodejs" -ErrorAction SilentlyContinue

# Проверить результат
Test-Path "$env:LOCALAPPDATA\Temp\eas-cli-nodejs"
# Должно вернуть: False
```

### Вариант B: Через Проводник Windows

1. Закройте все терминалы
2. Откройте Проводник Windows
3. Введите в адресной строке:
   ```
   %LOCALAPPDATA%\Temp\eas-cli-nodejs
   ```
4. Удалите всю папку `eas-cli-nodejs` (может потребоваться права администратора)

---

## ✅ Решение 3: Добавить исключение в антивирус

Проблема часто возникает из-за антивируса, блокирующего доступ к временным файлам.

### Windows Defender:

1. Откройте **Защита Windows** (Windows Security)
2. Перейдите в **Защита от вирусов и угроз**
3. Нажмите **Управление настройками**
4. Прокрутите вниз до **Исключения**
5. Нажмите **Добавить или удалить исключения**
6. Добавьте папку:
   ```
   C:\Users\ivany\AppData\Local\Temp\eas-cli-nodejs
   ```

### Другие антивирусы:
Добавьте исключение для папки `%LOCALAPPDATA%\Temp\eas-cli-nodejs`

---

## ✅ Решение 4: Использовать другой временный каталог

Создайте скрипт `build-with-custom-temp.ps1`:

```powershell
# Создать временную папку в другом месте
$customTemp = "C:\Temp\eas-build"
New-Item -ItemType Directory -Force -Path $customTemp | Out-Null

# Установить переменные окружения
$env:TEMP = $customTemp
$env:TMP = $customTemp

# Запустить build
cd apps/mobile
pnpm build:android
```

---

## ✅ Решение 5: Обновить EAS CLI

Возможно, проблема в версии EAS CLI:

```bash
cd apps/mobile
pnpm add -D eas-cli@latest
```

---

## 🔍 Диагностика

### Проверить, что блокирует доступ:

```powershell
# Проверить права доступа
icacls "$env:LOCALAPPDATA\Temp\eas-cli-nodejs"

# Проверить занятые файлы
Get-Process | Where-Object {$_.Path -like "*eas*" -or $_.Path -like "*node*"}
```

### Проверить антивирус логи:
- Откройте журнал событий Windows Defender
- Проверьте, не блокируется ли доступ к папке Temp

---

## 🚀 Рекомендуемый порядок действий

1. ✅ **Закройте все терминалы и процессы Node/EAS**
2. ✅ **Запустите скрипт очистки** (`.\clear-eas-cache.ps1` или `pnpm eas:clear-cache`)
3. ✅ **Добавьте исключение в антивирус** (если используется)
4. ✅ **Откройте PowerShell от имени администратора**
5. ✅ **Запустите build**: `pnpm build:android`

---

## 💡 Альтернатива: Использовать локальную сборку

Если проблема не решается, можно собрать APK локально (debug версия):

```bash
cd apps/mobile
pnpm android
```

Это создаст APK в `android/app/build/outputs/apk/debug/app-debug.apk`

Для production APK все равно потребуется EAS Build или настройка signing вручную.

---

## 📝 Примечания

- Проблема часто возникает из-за **антивируса** или **Windows Defender**
- Всегда запускайте команды от имени администратора при работе с EAS Build на Windows
- Временные файлы EAS CLI создаются в `%LOCALAPPDATA%\Temp\eas-cli-nodejs\`

