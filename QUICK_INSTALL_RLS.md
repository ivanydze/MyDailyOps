# 🚀 Быстрая установка RLS политик

## Вариант 1: Для TEXT типа user_id (обычно)

Скопируйте и выполните в Supabase SQL Editor файл **`INSTALL_RLS_POLICIES.sql`**

---

## Вариант 2: Для UUID типа user_id

Скопируйте и выполните в Supabase SQL Editor файл **`INSTALL_RLS_POLICIES_UUID.sql`**

---

## Как определить, какой вариант использовать?

Выполните в Supabase SQL Editor:

```sql
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'tasks' AND column_name = 'user_id';
```

- Если `data_type = 'text'` или `data_type = 'character varying'` → используйте **Вариант 1**
- Если `data_type = 'uuid'` → используйте **Вариант 2**

---

## После установки

1. Проверьте, что все 4 политики созданы (скрипт покажет результат)
2. Запустите тесты: `.\RUN_SECURITY_TEST.bat`
3. Все 9 тестов должны пройти ✅

