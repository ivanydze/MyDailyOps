# Инструкция по запуску приложений MyDailyOps

## 📋 Предварительные требования

Убедитесь, что установлены:
- **Node.js** (версия 18 или выше)
- **pnpm** (версия 8 или выше)
- Для Mobile: **Expo CLI** (установится автоматически)
- Для Desktop: **Rust** и **Tauri CLI** (для компиляции)

## 🚀 Быстрый старт

### Вариант 1: Запуск из корня проекта

```bash
# Перейдите в корневую директорию проекта
cd C:\Dev\MyDailyOps

# Установите зависимости (если еще не установлены)
pnpm install

# Запустите Desktop приложение
pnpm dev:desktop

# ИЛИ запустите Mobile приложение (в другом терминале)
pnpm dev:mobile
```

---

## 🖥️ Desktop приложение (Tauri + React)

### Запуск из корня проекта:
```bash
cd C:\Dev\MyDailyOps
pnpm dev:desktop
```

### Запуск из папки desktop:
```bash
cd C:\Dev\MyDailyOps\apps\desktop
pnpm tauri:dev
```

### Что происходит:
1. Запускается Vite dev server на порту **1420**
2. Компилируется Rust backend (Tauri)
3. Открывается окно Tauri приложения

### Возможные проблемы:

#### ❌ Ошибка: "Port 1420 is already in use"
**Решение:**
```bash
# Найти процесс на порту 1420
netstat -ano | findstr :1420

# Остановить процесс (замените PID на реальный номер)
taskkill /F /PID <PID>
```

#### ❌ Ошибка: "Command not found"
**Решение:** Убедитесь, что вы в корне проекта, или используйте команду из папки desktop:
```bash
cd apps\desktop
pnpm tauri:dev
```

---

## 📱 Mobile приложение (React Native + Expo)

### Запуск из корня проекта:
```bash
cd C:\Dev\MyDailyOps
pnpm dev:mobile
```

### Запуск из папки mobile:
```bash
cd C:\Dev\MyDailyOps\apps\mobile
pnpm start
# или
npx expo start
```

### Что происходит:
1. Запускается Metro bundler (Expo)
2. Открывается Expo DevTools в браузере
3. Появится QR-код для подключения

### Как подключиться:

#### На физическом устройстве:
1. Установите **Expo Go** из App Store (iOS) или Google Play (Android)
2. Отсканируйте QR-код из терминала или браузера
3. Приложение откроется в Expo Go

#### На эмуляторе/симуляторе:
- **Android:** Нажмите `a` в терминале или выберите "Run on Android device/emulator" в DevTools
- **iOS (только macOS):** Нажмите `i` в терминале или выберите "Run on iOS simulator" в DevTools

### Дополнительные команды:
```bash
# Очистить кеш Metro bundler
npx expo start -c

# Запустить на Android эмуляторе
npx expo run:android

# Запустить на iOS симуляторе (только macOS)
npx expo run:ios
```

---

## 🔄 Запуск обоих приложений одновременно

### Способ 1: Два терминала

**Терминал 1 (Desktop):**
```bash
cd C:\Dev\MyDailyOps
pnpm dev:desktop
```

**Терминал 2 (Mobile):**
```bash
cd C:\Dev\MyDailyOps
pnpm dev:mobile
```

### Способ 2: Использовать Turbo (если настроено)
```bash
cd C:\Dev\MyDailyOps
pnpm dev
```

---

## 📝 Полезные команды

### Установка зависимостей:
```bash
# Установить все зависимости
pnpm install

# Установить зависимости для конкретного приложения
pnpm --filter @mydailyops/desktop install
pnpm --filter mydailyops-mobile install
```

### Сборка для production:
```bash
# Desktop
cd apps\desktop
pnpm tauri:build

# Mobile
cd apps\mobile
pnpm build
```

### Очистка:
```bash
# Desktop - очистить build файлы
cd apps\desktop
pnpm clean

# Mobile - очистить кеш
cd apps\mobile
npx expo start -c
```

---

## 🐛 Решение проблем

### Desktop не запускается:
1. Проверьте, установлен ли Rust: `rustc --version`
2. Проверьте, установлен ли Tauri CLI: `pnpm tauri --version`
3. Очистите build: `cd apps\desktop && pnpm clean`
4. Переустановите зависимости: `pnpm install`

### Mobile не запускается:
1. Очистите кеш Metro: `npx expo start -c`
2. Проверьте, установлен ли Expo CLI: `npx expo --version`
3. Удалите `node_modules` и переустановите: 
   ```bash
   rm -rf node_modules
   pnpm install
   ```

### Проблемы с портами:
- **Desktop:** Порт 1420 занят → убейте процесс (см. выше)
- **Mobile:** Порт 8081 занят → измените порт или убейте процесс

---

## 📍 Структура проекта

```
MyDailyOps/
├── apps/
│   ├── desktop/          # Desktop приложение (Tauri + React)
│   │   ├── src/
│   │   ├── src-tauri/
│   │   └── package.json
│   └── mobile/           # Mobile приложение (React Native + Expo)
│       ├── app/
│       ├── components/
│       └── package.json
├── packages/
│   └── core/             # Общая логика
├── package.json          # Корневой package.json
└── pnpm-workspace.yaml
```

---

## ✅ Чеклист первого запуска

- [ ] Установлен Node.js (18+)
- [ ] Установлен pnpm (`npm install -g pnpm`)
- [ ] Клонирован репозиторий
- [ ] Выполнена команда `pnpm install` в корне проекта
- [ ] Для Desktop: установлен Rust и Tauri CLI
- [ ] Для Mobile: установлен Expo Go на устройстве (опционально)

---

## 🎯 Быстрые команды

| Приложение | Команда из корня | Команда из папки app |
|------------|------------------|---------------------|
| **Desktop** | `pnpm dev:desktop` | `cd apps/desktop && pnpm tauri:dev` |
| **Mobile** | `pnpm dev:mobile` | `cd apps/mobile && pnpm start` |
| **Оба** | Два терминала | - |

---

## 💡 Советы

1. **Всегда запускайте из корня проекта** для использования workspace команд
2. **Используйте два терминала** для одновременного запуска Desktop и Mobile
3. **Очищайте кеш** если приложение ведет себя странно: `npx expo start -c` для Mobile
4. **Проверяйте порты** если возникают ошибки запуска
5. **Для Desktop** первый запуск может занять время (компиляция Rust)

---

## 🔗 Полезные ссылки

- [Tauri документация](https://tauri.app/)
- [Expo документация](https://docs.expo.dev/)
- [pnpm документация](https://pnpm.io/)

---

**Готово!** Теперь вы можете запускать оба приложения. Если возникнут проблемы, проверьте раздел "Решение проблем" выше.

