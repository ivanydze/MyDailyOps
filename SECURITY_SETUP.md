# 🔒 Руководство по настройке безопасности приложения MyDailyOps

## ✅ Что уже реализовано в коде

### 1. Фильтрация по `user_id` при загрузке задач
- ✅ Все запросы к базе данных фильтруют задачи по `user_id` текущего пользователя
- ✅ `loadTasksFromCache(userId)` - загружает только задачи текущего пользователя
- ✅ Supabase запросы используют `.eq('user_id', userId)`

### 2. Проверки безопасности при операциях
- ✅ **Добавление задач**: Автоматически привязывается к `user_id` текущего пользователя
- ✅ **Обновление задач**: Проверяется, что задача принадлежит текущему пользователю
- ✅ **Удаление задач**: Проверяется, что задача принадлежит текущему пользователю
- ✅ **Получение задачи по ID**: Фильтруется по `user_id` - невозможно получить чужие задачи

### 3. Функции с проверками безопасности

#### Desktop (`apps/desktop/src/`)
- ✅ `getTaskById(id, userId)` - требует `userId` для фильтрации
- ✅ `deleteTaskFromCache(id, userId)` - удаляет только задачи пользователя
- ✅ `updateTask` - проверяет принадлежность перед обновлением
- ✅ `deleteTask` - проверяет принадлежность перед удалением

#### Mobile (`apps/mobile/`)
- ✅ `getTaskById(id, userId)` - требует `userId` для фильтрации
- ✅ `deleteTaskFromCache(id, userId)` - удаляет только задачи пользователя
- ✅ `updateTask` - проверяет принадлежность перед обновлением
- ✅ `deleteTask` - проверяет принадлежность перед удалением

---

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Настройка Row Level Security (RLS) в Supabase

**Приложение защищено на уровне кода, но НЕОБХОДИМО настроить RLS в Supabase** для дополнительной защиты на уровне базы данных.

### Почему это важно?
- Даже если код правильно фильтрует данные, RLS защищает от:
  - SQL-инъекций
  - Прямых запросов к базе данных (если кто-то получит доступ к ключу)
  - Ошибок в коде приложения

---

## 📋 Пошаговая инструкция настройки RLS в Supabase

### Шаг 1: Войдите в Supabase Dashboard

1. Откройте [https://app.supabase.com](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **Authentication** → **Policies** или **Database** → **Tables** → **tasks**

### Шаг 2: Включите Row Level Security для таблицы `tasks`

1. В Supabase Dashboard перейдите в **Database** → **Tables**
2. Найдите таблицу `tasks` и откройте её
3. Перейдите на вкладку **Policies**
4. Нажмите **Enable RLS** (если еще не включено)

### Шаг 3: Создайте политики для таблицы `tasks`

#### Политика 1: SELECT (чтение задач)

```sql
-- Название: Users can view only their own tasks
-- Команда: SELECT

CREATE POLICY "Users can view only their own tasks"
ON tasks
FOR SELECT
USING (auth.uid() = user_id);
```

**Что делает:** Позволяет пользователям видеть только свои задачи

---

#### Политика 2: INSERT (добавление задач)

```sql
-- Название: Users can insert only their own tasks
-- Команда: INSERT

CREATE POLICY "Users can insert only their own tasks"
ON tasks
FOR INSERT
WITH CHECK (auth.uid() = user_id);
```

**Что делает:** Позволяет пользователям создавать только задачи с их `user_id`

---

#### Политика 3: UPDATE (обновление задач)

```sql
-- Название: Users can update only their own tasks
-- Команда: UPDATE

-- Если user_id имеет тип TEXT:
CREATE POLICY "Users can update only their own tasks"
ON tasks
FOR UPDATE
USING (auth.uid()::text = user_id)
WITH CHECK (auth.uid()::text = user_id);

-- ИЛИ если user_id имеет тип UUID:
CREATE POLICY "Users can update only their own tasks"
ON tasks
FOR UPDATE
USING (auth.uid() = user_id)
WITH CHECK (auth.uid() = user_id);
```

**Что делает:** 
- `USING` - проверяет, что задача принадлежит пользователю перед обновлением
- `WITH CHECK` - проверяет, что после обновления `user_id` не изменился

**⚠️ ВАЖНО:** Проверьте тип поля `user_id` в вашей таблице и используйте правильное приведение типов!

---

#### Политика 4: DELETE (удаление задач)

```sql
-- Название: Users can delete only their own tasks
-- Команда: DELETE

-- Если user_id имеет тип TEXT:
CREATE POLICY "Users can delete only their own tasks"
ON tasks
FOR DELETE
USING (auth.uid()::text = user_id);

-- ИЛИ если user_id имеет тип UUID:
CREATE POLICY "Users can delete only their own tasks"
ON tasks
FOR DELETE
USING (auth.uid() = user_id);
```

**Что делает:** Позволяет пользователям удалять только свои задачи

**⚠️ ВАЖНО:** Проверьте тип поля `user_id` в вашей таблице и используйте правильное приведение типов!

---

### Шаг 4: Примените политики через SQL Editor (альтернативный способ)

Если у вас нет доступа к визуальному редактору политик, используйте SQL Editor:

1. Перейдите в **SQL Editor** в Supabase Dashboard
2. Создайте новый запрос
3. Скопируйте и выполните следующий SQL:

```sql
-- Включить RLS для таблицы tasks
ALTER TABLE tasks ENABLE ROW LEVEL SECURITY;

-- Удалить все существующие политики (если есть)
DROP POLICY IF EXISTS "Users can view only their own tasks" ON tasks;
DROP POLICY IF EXISTS "Users can insert only their own tasks" ON tasks;
DROP POLICY IF EXISTS "Users can update only their own tasks" ON tasks;
DROP POLICY IF EXISTS "Users can delete only their own tasks" ON tasks;

-- Создать политики
CREATE POLICY "Users can view only their own tasks"
ON tasks FOR SELECT
USING (auth.uid() = user_id);

CREATE POLICY "Users can insert only their own tasks"
ON tasks FOR INSERT
WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update only their own tasks"
ON tasks FOR UPDATE
USING (auth.uid() = user_id)
WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can delete only their own tasks"
ON tasks FOR DELETE
USING (auth.uid() = user_id);
```

---

### Шаг 5: Проверьте работу RLS

1. **Тест 1: Войдите под первым пользователем**
   - Создайте задачу
   - Проверьте, что она видна в списке

2. **Тест 2: Войдите под вторым пользователем**
   - Убедитесь, что задача первого пользователя НЕ видна
   - Создайте свою задачу

3. **Тест 3: Попробуйте обновить чужую задачу (через API)**
   - Это должно вернуть ошибку доступа

---

## 🔍 Проверка текущих политик

Чтобы проверить, какие политики уже установлены, выполните в SQL Editor:

```sql
-- Посмотреть все политики для таблицы tasks
SELECT 
  schemaname,
  tablename,
  policyname,
  permissive,
  roles,
  cmd,
  qual,
  with_check
FROM pg_policies
WHERE tablename = 'tasks';
```

---

## 📝 Дополнительные рекомендации

### 1. Категории (categories)

Если у вас есть таблица `categories`, также настройте для неё RLS:

```sql
ALTER TABLE categories ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view only their own categories"
ON categories FOR SELECT
USING (auth.uid() = user_id);

CREATE POLICY "Users can insert only their own categories"
ON categories FOR INSERT
WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update only their own categories"
ON categories FOR UPDATE
USING (auth.uid() = user_id)
WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can delete only their own categories"
ON categories FOR DELETE
USING (auth.uid() = user_id);
```

### 2. Логирование и мониторинг

Настройте логирование в Supabase для отслеживания попыток несанкционированного доступа:

1. Перейдите в **Logs** → **Postgres Logs**
2. Включите логирование всех операций

### 3. Регулярная проверка безопасности

- Периодически проверяйте логи на подозрительную активность
- Тестируйте приложение с несколькими пользователями
- Обновляйте зависимости регулярно

---

## ⚡ Быстрая проверка безопасности

Выполните следующие проверки:

### ✅ Проверка 1: Загрузка задач
- [ ] Пользователь видит только свои задачи
- [ ] Задачи других пользователей не отображаются

### ✅ Проверка 2: Создание задачи
- [ ] Созданная задача имеет правильный `user_id`
- [ ] Задача не видна другим пользователям

### ✅ Проверка 3: Обновление задачи
- [ ] Можно обновить только свою задачу
- [ ] Попытка обновить чужую задачу возвращает ошибку

### ✅ Проверка 4: Удаление задачи
- [ ] Можно удалить только свою задачу
- [ ] Попытка удалить чужую задачу возвращает ошибку

### ✅ Проверка 5: RLS в Supabase
- [ ] RLS включен для таблицы `tasks`
- [ ] Созданы все 4 политики (SELECT, INSERT, UPDATE, DELETE)

---

## 🆘 Если что-то не работает

### Проблема: Пользователь видит задачи других пользователей

**Решение:**
1. Проверьте, что RLS включен: `ALTER TABLE tasks ENABLE ROW LEVEL SECURITY;`
2. Проверьте политику SELECT: `SELECT * FROM pg_policies WHERE tablename = 'tasks';`
3. Убедитесь, что `user_id` в задачах совпадает с `auth.uid()`

### Проблема: Не могу создать задачу

**Решение:**
1. Проверьте политику INSERT
2. Убедитесь, что `user_id` устанавливается правильно в коде
3. Проверьте логи Supabase на ошибки

### Проблема: Ошибка "new row violates row-level security policy"

**Решение:**
- Это нормально! Это означает, что RLS работает и блокирует несанкционированный доступ
- Проверьте, что `user_id` в запросе совпадает с `auth.uid()`

---

## 📚 Полезные ссылки

- [Supabase RLS Documentation](https://supabase.com/docs/guides/auth/row-level-security)
- [PostgreSQL RLS Documentation](https://www.postgresql.org/docs/current/ddl-rowsecurity.html)
- [Supabase Auth Reference](https://supabase.com/docs/reference/javascript/auth)

---

## ✅ Итоговый чеклист безопасности

После выполнения всех шагов убедитесь:

- [ ] ✅ Код приложения проверяет `user_id` во всех операциях
- [ ] ✅ RLS включен для таблицы `tasks` в Supabase
- [ ] ✅ Созданы все 4 политики (SELECT, INSERT, UPDATE, DELETE)
- [ ] ✅ Протестировано с несколькими пользователями
- [ ] ✅ Задачи изолированы между пользователями
- [ ] ✅ Невозможно удалить или изменить чужие задачи
- [ ] ✅ Логирование включено для мониторинга

**После выполнения всех пунктов ваше приложение полностью защищено!** 🎉

