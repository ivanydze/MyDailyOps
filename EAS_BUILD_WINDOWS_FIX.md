# 🔧 EAS Build Windows Permission Fix

## Проблема
```
EPERM: operation not permitted, rmdir
Failed to upload the project tarball to EAS Build
```

Это типичная проблема Windows с правами доступа к временным файлам EAS CLI.

---

## 🔧 Решения (попробуйте в порядке приоритета)

### Решение 1: Очистить кеш EAS CLI ✅ (Рекомендуется)

```bash
cd apps/mobile

# Очистить кеш EAS CLI
npx eas-cli config --clear-cache

# Или вручную удалить временную папку
# (может потребоваться закрыть все терминалы и процессы)
```

### Решение 2: Запустить от имени администратора

1. Закройте текущий терминал PowerShell
2. Откройте **новый PowerShell от имени администратора** (ПКМ → "Запуск от имени администратора")
3. Перейдите в директорию проекта:
   ```powershell
   cd C:\Dev\MyDailyOps\apps\mobile
   ```
4. Запустите build снова:
   ```bash
   pnpm build:android
   ```

### Решение 3: Добавить исключение в антивирус

Если используете Windows Defender или другой антивирус:

1. Откройте **Windows Security** (Защита Windows)
2. Перейдите в **Virus & threat protection**
3. **Manage settings** → **Exclusions**
4. Добавьте исключение для:
   - `C:\Users\ivany\AppData\Local\Temp\eas-cli-nodejs\`
   - Или всей папки `C:\Users\ivany\AppData\Local\Temp\`

### Решение 4: Удалить временные папки вручную

```powershell
# Закройте все терминалы и процессы EAS/Node
# Затем выполните:

Remove-Item -Recurse -Force "$env:LOCALAPPDATA\Temp\eas-cli-nodejs" -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force "$env:TEMP\eas-cli-nodejs" -ErrorAction SilentlyContinue
```

### Решение 5: Использовать другой временный каталог

Установите переменную окружения для другого временного каталога:

```powershell
# В PowerShell перед запуском build
$env:TEMP = "C:\Temp"
$env:TMP = "C:\Temp"
New-Item -ItemType Directory -Force -Path "C:\Temp"

# Затем запустите build
pnpm build:android
```

### Решение 6: Проверить занятые файлы

Убедитесь, что нет запущенных процессов EAS/Node:

```powershell
# Проверить процессы Node
Get-Process node -ErrorAction SilentlyContinue | Stop-Process -Force

# Проверить процессы EAS
Get-Process | Where-Object {$_.ProcessName -like "*eas*" -or $_.ProcessName -like "*node*"}
```

---

## ✅ Рекомендуемый порядок действий

1. **Очистить кеш EAS CLI**
2. **Запустить от имени администратора**
3. **Добавить исключение в антивирус** (если проблема повторяется)
4. **Попробовать снова**

---

## 🚀 После исправления

```bash
cd apps/mobile
pnpm build:android
```

---

## 📝 Дополнительные проверки

### Проверка прав доступа к папке Temp

```powershell
# Проверить права
icacls "$env:LOCALAPPDATA\Temp"

# Если нужно, дать полные права (только если уверены)
icacls "$env:LOCALAPPDATA\Temp" /grant "$env:USERNAME:(OI)(CI)F"
```

---

## 💡 Альтернатива: Использовать локальную сборку

Если проблема с EAS Build продолжается, можно собрать APK локально:

```bash
# Локальная сборка APK
cd apps/mobile
pnpm android
```

Это создаст debug APK локально, но для production APK все равно нужен EAS Build или ручная настройка signing.

